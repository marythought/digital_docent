<%= form_for(@medium) do |f| %>
  <% if @medium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medium.errors.count, "error") %> prohibited this medium from being saved:</h2>

      <ul>
      <% @medium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :file_link %><br>
    <%= f.text_field :file_link %>
  </div>

  <div class=field>
    <%= f.grouped_collection_select :global_multimediable, [ Item, Artist, Room ], :all, :model_name, :to_global_id, :name %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
